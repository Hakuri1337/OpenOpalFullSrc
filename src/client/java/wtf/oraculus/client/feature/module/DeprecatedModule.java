package wtf.oraculus.client.feature.module;

/**
 * Marks backwards-compatible modules that are hidden from module GUIs unless a config explicitly exposes them.
 */
public interface DeprecatedModule {
}
